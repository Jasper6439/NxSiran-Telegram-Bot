# LoveSupremacy Bot - e2-micro 轻量化依赖
# v1.6.6 优化版：移除重型依赖，极限减负

# Bot 框架
python-telegram-bot==22.7

# HTTP 客户端（异步）
httpx>=0.27.0
aiohttp>=3.9.0

# 图片处理
Pillow>=10.0.0

# 音视频下载
yt-dlp>=2024.1.1

# 记忆系统 - LightRAG（本地向量存储）
lightrag-hku>=1.0.0

# 关键词检索 - BM25（补充向量检索）
rank-bm25>=0.2

# 语音合成 - Edge TTS（云端 API）
edge-tts>=6.1.0

# 数值计算（轻量）
numpy>=1.24.0

# 内存监控
psutil>=5.9.0
APScheduler>=3.10.0

# Web 框架 (v1.7 FastAPI 迁移)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# 压力测试 (v1.7 Phase 5)
locust>=2.20.0
