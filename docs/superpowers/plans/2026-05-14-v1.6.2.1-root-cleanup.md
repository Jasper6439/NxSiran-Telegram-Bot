# v1.6.2.1 根目录整理 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将根目录中碎片化散落的模块文件归入正确的目录，删除冗余/废弃文件，清理过时注释，使项目结构清晰规范。

**Architecture:** 按职责将文件分为 4 个目标目录：`characters/`（角色相关）、`system/`（系统级模块）、`game_api/`（游戏相关）、`tools/_archive/`（归档）。所有移动操作通过 `git mv` 保留历史，移动后更新所有 import 引用，并在原位置保留 re-export shim 确保向后兼容。

**Tech Stack:** Python, Git, aiohttp

---

## 当前根目录文件分类总览

### 需要移动的文件（共 20 个）

| 文件 | 目标目录 | 引用数 | 风险 |
|---|---|---|---|
| `ai_client.py` | `characters/` | 8 | 高 |
| `ai_compete.py` | `characters/` | 2 | 中 |
| `ai_core.py` | `characters/` | 1 | 低 |
| `anniversary.py` | `characters/` | 9 | 高 |
| `chat_engine.py` | `characters/` | 0 | 低 |
| `chat_history.py` | `characters/` | 16 | 高 |
| `emotion.py` | `characters/` | 7 | 高 |
| `emotion_analyzer.py` | `characters/` | 0 | 低 |
| `auth.py` | `system/` | 34 | 高 |
| `scheduler.py` | `system/` | 1 | 低 |
| `email_sender.py` | `system/` | 1 | 低 |
| `awakening_detector.py` | `game_api/` | 0 | 低 |
| `image_gen.py` | `characters/` | 11 | 高 |
| `memory_legacy.py` | `characters/` | 10 | 高 |
| `qdrant_memory.py` | `characters/` | 5 | 中 |
| `tts_engine.py` | `characters/` | 4 | 中 |
| `weather.py` | `characters/` | 3 | 中 |
| `music_skill.py` | `characters/` | 2 | 中 |
| `novel_knowledge.py` | `characters/` | 2 | 中 |
| `stats.py` | `characters/` | 12 | 高 |

### 需要删除的文件（共 3 个）

| 文件 | 原因 |
|---|---|
| `bridge_server.py` | 功能已被 webhook_server.py 完全覆盖 |
| `auth_v3.py` | 合并到 auth.py 后删除 |
| `migrate_to_sqlite.py` | 一次性迁移脚本，已完成 |

### 需要归档的文件（共 3 个）

| 文件 | 原因 |
|---|---|
| `bot_instance.py` | 未启用的多实例架构 |
| `bot_manager.py` | 未启用的多实例架构 |
| `bots_config.json` | 未启用的多实例配置（含 token） |

### 需要移动的脚本/服务文件（共 6 个）

| 文件 | 目标 |
|---|---|
| `auto_restart_watcher.sh` | `system/` |
| `cloudflared-quick.service` | `system/` |
| `start-tunnel.sh` | `system/` |
| `update-tunnel-url.sh` | `system/` |
| `nxsiran-bot.service` | `system/` |
| `nxsiran-bridge.service` | `system/` |
| `nxsiran-watcher.service` | `system/` |
| `nxsiran-webhook.service` | `system/` |
| `init_admin.py` | `tools/` |
| `bot.py.backup` | 删除 |

### 需要清理的文件

| 文件 | 操作 |
|---|---|
| `bot.py` | 清理过时注释（1-31行 changelog、Phase/Skill 标记注释） |

---

## 目标目录结构

```
love-supremacy-zone/
├── bot.py                    # 入口（清理后）
├── config.py                 # 配置（不动）
├── prompts.py                # 提示词（不动）
├── webhook_server.py         # Webhook+Bridge 服务（不动）
├── requirements.txt          # 依赖（不动）
├── Dockerfile                # 部署（不动）
├── docker-compose.yml        # 部署（不动）
│
├── characters/               # 角色系统（扩展）
│   ├── __init__.py           # 角色注册表（不动）
│   ├── base.py               # 角色基类（不动）
│   ├── chayewoon.py          # 车如云角色（不动）
│   ├── ai_client.py          # ← 从根目录移入
│   ├── ai_compete.py         # ← 从根目录移入
│   ├── ai_core.py            # ← 从根目录移入
│   ├── anniversary.py        # ← 从根目录移入
│   ├── chat_engine.py        # ← 从根目录移入
│   ├── chat_history.py       # ← 从根目录移入
│   ├── emotion.py            # ← 从根目录移入
│   ├── emotion_analyzer.py   # ← 从根目录移入
│   ├── image_gen.py          # ← 从根目录移入
│   ├── memory_legacy.py      # ← 从根目录移入
│   ├── qdrant_memory.py      # ← 从根目录移入
│   ├── tts_engine.py         # ← 从根目录移入
│   ├── weather.py            # ← 从根目录移入
│   ├── music_skill.py        # ← 从根目录移入
│   ├── novel_knowledge.py    # ← 从根目录移入
│   ├── stats.py              # ← 从根目录移入
│   └── chayewoon/            # 角色数据（不动）
│
├── system/                   # 系统级模块（新建）
│   ├── __init__.py           # 包初始化
│   ├── auth.py               # ← 从根目录移入（合并 auth_v3）
│   ├── scheduler.py          # ← 从根目录移入
│   ├── email_sender.py       # ← 从根目录移入
│   ├── auto_restart_watcher.sh
│   ├── cloudflared-quick.service
│   ├── start-tunnel.sh
│   ├── update-tunnel-url.sh
│   ├── nxsiran-bot.service
│   ├── nxsiran-bridge.service
│   ├── nxsiran-watcher.service
│   └── nxsiran-webhook.service
│
├── game_api/                 # 游戏 API（扩展）
│   ├── ...（现有文件不动）
│   └── awakening_detector.py # ← 从根目录移入
│
├── tools/                    # 工具脚本（扩展）
│   ├── ...（现有文件不动）
│   ├── init_admin.py         # ← 从根目录移入
│   └── _archive/             # 归档目录（新建）
│       ├── bot_instance.py
│       ├── bot_manager.py
│       └── bots_config.json
│
├── database/                 # 数据库（不动）
├── packages/                 # 子包（不动）
├── web-v2/                   # 前端（不动）
├── knowledge/                # 知识库（不动）
├── docs/                     # 文档（不动）
└── static/                   # 静态资源（不动）
```

---

## Task 1: 创建目标目录结构

**Files:**
- Create: `system/__init__.py`
- Create: `tools/_archive/.gitkeep`

- [ ] **Step 1: 创建 system/ 目录和 __init__.py**

```python
# system/__init__.py
"""
系统级模块 - 认证、调度、邮件、运维脚本
"""
```

- [ ] **Step 2: 创建 tools/_archive/ 归档目录**

```bash
mkdir -p tools/_archive
touch tools/_archive/.gitkeep
```

- [ ] **Step 3: Commit**

```bash
git add system/__init__.py tools/_archive/.gitkeep
git commit -m "chore(v1.6.2.1): 创建 system/ 和 tools/_archive/ 目录结构"
```

---

## Task 2: 归档废弃文件

**Files:**
- Move: `bot_instance.py` → `tools/_archive/bot_instance.py`
- Move: `bot_manager.py` → `tools/_archive/bot_manager.py`
- Move: `bots_config.json` → `tools/_archive/bots_config.json`
- Delete: `bridge_server.py`
- Delete: `migrate_to_sqlite.py`
- Delete: `bot.py.backup`

- [ ] **Step 1: 归档多实例架构文件**

```bash
git mv bot_instance.py tools/_archive/bot_instance.py
git mv bot_manager.py tools/_archive/bot_manager.py
git mv bots_config.json tools/_archive/bots_config.json
```

- [ ] **Step 2: 删除冗余文件**

```bash
git rm bridge_server.py
git rm migrate_to_sqlite.py
git rm bot.py.backup
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore(v1.6.2.1): 归档废弃的多实例架构文件，删除冗余的 bridge_server/migrate/bot.backup"
```

---

## Task 3: 合并 auth_v3.py 到 auth.py 并移入 system/

**Files:**
- Modify: `auth.py` (合并 v3 功能后移入 `system/`)
- Delete: `auth_v3.py`
- Create: 根目录 `auth.py` re-export shim

**合并策略：** auth.py 当前是 v2.5（chat_id 为主键），auth_v3 是 v3（UUID + email_index 为主键）。auth_v3 设计更完善但从未被集成。合并方式：**保留 auth.py 的 v2.5 数据结构不变**（因为 34 处引用都在用），仅从 auth_v3 中提取以下增强功能合并进来：
1. `register_user_v3` 风格的邮箱注册函数（新增 `register_user_by_email`）
2. `validate_user_v3` 风格的邮箱登录支持（增强现有 `validate_user`）
3. `get_user_by_email` 函数（新增）
4. `get_user_by_id` 函数（新增）
5. `is_admin_user_v3` 函数（新增 `is_admin_user_by_id`）

**不合并的部分：** v3 的 UUID 主键体系、`_migrate_users_v3` 迁移逻辑、`sessions_v3.json` 持久化（当前已有 `sessions.json`）。

- [ ] **Step 1: 在 auth.py 末尾添加从 auth_v3 提取的增强函数**

在 `auth.py` 的 `reset_password` 函数之后添加：

```python
# ============================================================
# v3 增强功能（从 auth_v3.py 合并）
# ============================================================

def register_user_by_email(email, username, password):
    """
    通过邮箱注册新用户（v3 增强）
    返回: (success: bool, message: str)
    """
    if not email or not username or not password:
        return False, "邮箱、用户名和密码不能为空"

    if len(password) < 6:
        return False, "密码长度至少 6 位"

    if len(username) < 2 or len(username) > 20:
        return False, "用户名长度 2-20 个字符"

    if "@" not in email or "." not in email.split("@")[-1]:
        return False, "邮箱格式不正确"

    email = email.lower().strip()
    users = _get_users_dict()

    # 检查邮箱是否已被使用
    for u in users.values():
        if u.get("email", "").lower() == email:
            return False, "该邮箱已注册，请直接登录"

    # 检查用户名是否已被使用
    for u in users.values():
        if u.get("username", "").lower() == username.lower():
            return False, "用户名已被使用"

    config = load_config()
    role = "admin" if username == config.get("admin_username", "Ulysses") else "user"

    now = datetime.now(get_default_tz()).isoformat()
    # 使用邮箱前缀作为临时 key（首次绑定 chat_id 时会迁移）
    temp_key = f"email_{secrets.token_hex(8)}"
    users[temp_key] = {
        "username": username,
        "email": email,
        "password_hash": hash_password(password),
        "display_name": username.capitalize(),
        "role": role,
        "email_verified": False,
        "created_at": now,
        "last_login": None,
        "login_count": 0,
        "preferences": {"language": "zh-CN", "theme": "auto"},
        "character_bindings": {},
        "reset_code": None,
        "reset_code_expires": None,
    }

    _save_users_dict(users)
    logging.info(f"[用户系统] 邮箱注册: {username} ({email}, role: {role})")
    return True, "注册成功"


def get_user_by_email(email):
    """通过邮箱获取用户数据"""
    email = email.lower().strip()
    users = _get_users_dict()
    for u in users.values():
        if u.get("email", "").lower() == email:
            return u
    return None


def get_user_by_id(user_id):
    """通过 user_id 获取用户数据"""
    users = _get_users_dict()
    return users.get(str(user_id))


def is_admin_user_by_id(user_id):
    """通过 user_id 检查是否为管理员"""
    user = get_user_by_id(user_id)
    return user and user.get("role") == "admin"
```

- [ ] **Step 2: 移动 auth.py 到 system/ 并删除 auth_v3.py**

```bash
git mv auth.py system/auth.py
git rm auth_v3.py
```

- [ ] **Step 3: 创建根目录 auth.py re-export shim**

```python
# auth.py - re-export shim for backward compatibility
# 实际模块已移至 system/auth.py
from system.auth import *
```

- [ ] **Step 4: 验证所有 import 正常**

运行以下命令确认无 ImportError：

```bash
cd /workspace/love-supremacy-zone && python -c "from auth import validate_user, generate_session_token, is_admin_user; print('auth re-export OK')"
python -c "from system.auth import validate_user, register_user_by_email, get_user_by_email; print('system.auth direct OK')"
```

Expected: 两行都输出 OK

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor(v1.6.2.1): 合并 auth_v3 增强功能到 auth.py，移入 system/ 目录"
```

---

## Task 4: 移动角色相关模块到 characters/

**Files:**
- Move: `ai_client.py`, `ai_compete.py`, `ai_core.py`, `anniversary.py`, `chat_engine.py`, `chat_history.py`, `emotion.py`, `emotion_analyzer.py`, `image_gen.py`, `memory_legacy.py`, `qdrant_memory.py`, `tts_engine.py`, `weather.py`, `music_skill.py`, `novel_knowledge.py`, `stats.py` → `characters/`
- Create: 16 个 re-export shim 文件在根目录

**引用更新策略：** 移动文件后，在根目录创建 re-export shim（`from characters.xxx import *`），确保现有 34+ 处 import 不会断裂。后续版本可逐步清理 shim。

- [ ] **Step 1: 批量移动文件到 characters/**

```bash
cd /workspace/love-supremacy-zone
git mv ai_client.py characters/ai_client.py
git mv ai_compete.py characters/ai_compete.py
git mv ai_core.py characters/ai_core.py
git mv anniversary.py characters/anniversary.py
git mv chat_engine.py characters/chat_engine.py
git mv chat_history.py characters/chat_history.py
git mv emotion.py characters/emotion.py
git mv emotion_analyzer.py characters/emotion_analyzer.py
git mv image_gen.py characters/image_gen.py
git mv memory_legacy.py characters/memory_legacy.py
git mv qdrant_memory.py characters/qdrant_memory.py
git mv tts_engine.py characters/tts_engine.py
git mv weather.py characters/weather.py
git mv music_skill.py characters/music_skill.py
git mv novel_knowledge.py characters/novel_knowledge.py
git mv stats.py characters/stats.py
```

- [ ] **Step 2: 创建所有 re-export shim**

为每个移动的文件在根目录创建 shim：

```python
# ai_client.py - re-export shim
from characters.ai_client import *
```

```python
# ai_compete.py - re-export shim
from characters.ai_compete import *
```

```python
# ai_core.py - re-export shim
from characters.ai_core import *
```

```python
# anniversary.py - re-export shim
from characters.anniversary import *
```

```python
# chat_engine.py - re-export shim
from characters.chat_engine import *
```

```python
# chat_history.py - re-export shim
from characters.chat_history import *
```

```python
# emotion.py - re-export shim
from characters.emotion import *
```

```python
# emotion_analyzer.py - re-export shim
from characters.emotion_analyzer import *
```

```python
# image_gen.py - re-export shim
from characters.image_gen import *
```

```python
# memory_legacy.py - re-export shim
from characters.memory_legacy import *
```

```python
# qdrant_memory.py - re-export shim
from characters.qdrant_memory import *
```

```python
# tts_engine.py - re-export shim
from characters.tts_engine import *
```

```python
# weather.py - re-export shim
from characters.weather import *
```

```python
# music_skill.py - re-export shim
from characters.music_skill import *
```

```python
# novel_knowledge.py - re-export shim
from characters.novel_knowledge import *
```

```python
# stats.py - re-export shim
from characters.stats import *
```

- [ ] **Step 3: 修复 characters/ 内部的相对 import**

移动后，characters/ 内部的文件互相引用需要更新。检查并修复以下文件中的 import：

**characters/ai_core.py** — 当前 `from ai_client import ...` → 改为相对 import：
```python
# 修改前
from ai_client import call_ai, MAX_HISTORY_MESSAGES
# 修改后
from characters.ai_client import call_ai, MAX_HISTORY_MESSAGES
# 或者用相对 import
from .ai_client import call_ai, MAX_HISTORY_MESSAGES
```

**characters/ai_compete.py** — 当前 `from ai_client import ...` → 同上

**characters/chat_engine.py** — 当前 `from ai_client import ...` → 同上

**characters/chat_history.py** — 当前 `from qdrant_memory import ...` → 同上

**characters/ai_core.py** — 当前 `from chat_history import ...`, `from emotion import ...`, `from stats import ...`, `from memory_legacy import ...`, `from anniversary import ...`, `from weather import ...` → 全部改为相对 import

**characters/emotion.py** — 检查是否有对其他 characters/ 模块的引用

**characters/stats.py** — 检查是否有对其他 characters/ 模块的引用

**characters/prompts.py** — 如果 prompts.py 中引用了 `chat_history` 或 `emotion` 或 `stats`，需要更新（但 prompts.py 留在根目录不动，所以用 `from characters.xxx import ...`）

**characters/image_gen.py** — 检查是否有对 `chat_history` 的引用

**characters/memory_legacy.py** — 检查是否有对其他模块的引用

**characters/tts_engine.py** — 检查是否有对其他模块的引用

**重要：** 使用 `grep -rn "from ai_client\|from chat_history\|from emotion\|from stats\|from memory_legacy\|from anniversary\|from weather\|from image_gen\|from qdrant_memory\|from tts_engine\|from music_skill\|from novel_knowledge\|from ai_compete\|from ai_core\|from emotion_analyzer" characters/` 搜索所有需要修改的内部引用。

对于 characters/ 内部文件之间的引用，统一使用**相对 import**（`from .xxx import ...`）。
对于 characters/ 外部文件引用 characters/ 内模块，通过 re-export shim 保持兼容，无需修改。

- [ ] **Step 4: 验证所有 import 正常**

```bash
cd /workspace/love-supremacy-zone
python -c "from ai_client import call_ai; print('ai_client OK')"
python -c "from chat_history import save_chat_message; print('chat_history OK')"
python -c "from emotion import get_emotion_emoji; print('emotion OK')"
python -c "from stats import load_stats; print('stats OK')"
python -c "from anniversary import load_anniversaries; print('anniversary OK')"
python -c "from image_gen import generate_face_image; print('image_gen OK')"
python -c "from memory_legacy import load_json; print('memory_legacy OK')"
python -c "from weather import get_weather; print('weather OK')"
python -c "from tts_engine import TTSEngine; print('tts_engine OK')"
python -c "from music_skill import search_music; print('music_skill OK')"
python -c "from novel_knowledge import init_novel_knowledge; print('novel_knowledge OK')"
python -c "from ai_core import call_ai; print('ai_core OK')"
python -c "from scheduler import scheduler; print('scheduler OK')"
```

Expected: 全部输出 OK

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor(v1.6.2.1): 移动 16 个角色相关模块到 characters/ 目录，保留 re-export shim"
```

---

## Task 5: 移动系统级模块到 system/

**Files:**
- Move: `scheduler.py`, `email_sender.py` → `system/`
- Move: `auto_restart_watcher.sh`, `cloudflared-quick.service`, `start-tunnel.sh`, `update-tunnel-url.sh`, `nxsiran-bot.service`, `nxsiran-bridge.service`, `nxsiran-watcher.service`, `nxsiran-webhook.service` → `system/`
- Create: 2 个 re-export shim（scheduler.py, email_sender.py）

- [ ] **Step 1: 移动 Python 模块**

```bash
git mv scheduler.py system/scheduler.py
git mv email_sender.py system/email_sender.py
```

- [ ] **Step 2: 创建 re-export shim**

```python
# scheduler.py - re-export shim
from system.scheduler import *
```

```python
# email_sender.py - re-export shim
from system.email_sender import *
```

- [ ] **Step 3: 移动运维脚本和服务文件**

```bash
git mv auto_restart_watcher.sh system/auto_restart_watcher.sh
git mv cloudflared-quick.service system/cloudflared-quick.service
git mv start-tunnel.sh system/start-tunnel.sh
git mv update-tunnel-url.sh system/update-tunnel-url.sh
git mv nxsiran-bot.service system/nxsiran-bot.service
git mv nxsiran-bridge.service system/nxsiran-bridge.service
git mv nxsiran-watcher.service system/nxsiran-watcher.service
git mv nxsiran-webhook.service system/nxsiran-webhook.service
```

- [ ] **Step 4: 更新 systemd service 文件中的路径引用**

检查 `system/nxsiran-bot.service` 等文件中的 `ExecStart`、`WorkingDirectory` 路径是否需要更新（因为 service 文件本身移动了，但 systemd 的 `ExecStart` 使用绝对路径，不受影响）。如果 service 文件中引用了相对路径的脚本（如 `start-tunnel.sh`），需要更新。

```bash
# 检查 service 文件内容
grep -n "ExecStart\|ExecReload\|WorkingDirectory" system/nxsiran-*.service system/cloudflared-quick.service
```

如果有相对路径引用，更新为 `system/xxx.sh`。

- [ ] **Step 5: 移动 init_admin.py 到 tools/**

```bash
git mv init_admin.py tools/init_admin.py
```

- [ ] **Step 6: 验证**

```bash
cd /workspace/love-supremacy-zone
python -c "from scheduler import scheduler; print('scheduler OK')"
python -c "from email_sender import send_email; print('email_sender OK')"
```

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "refactor(v1.6.2.1): 移动系统级模块和运维脚本到 system/ 目录"
```

---

## Task 6: 移动 awakening_detector.py 到 game_api/

**Files:**
- Move: `awakening_detector.py` → `game_api/awakening_detector.py`
- Create: 根目录 re-export shim

- [ ] **Step 1: 移动文件**

```bash
git mv awakening_detector.py game_api/awakening_detector.py
```

- [ ] **Step 2: 创建 re-export shim**

```python
# awakening_detector.py - re-export shim
from game_api.awakening_detector import *
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "refactor(v1.6.2.1): 移动 awakening_detector 到 game_api/ 目录"
```

---

## Task 7: 清理 bot.py 过时注释

**Files:**
- Modify: `bot.py:1-31` (替换文件头 changelog)
- Modify: `bot.py:46-142` (清理 Phase/Skill 标记注释)

- [ ] **Step 1: 替换文件头注释**

将 bot.py 第 1-31 行的冗长 changelog 替换为简洁的文件描述：

```python
"""
恋爱至上主义区域 - Telegram Bot 入口
=====================================
Slim entry point - command/handler logic extracted to packages/.
Character modules in characters/, system modules in system/.
"""
```

- [ ] **Step 2: 清理 import 区域的过时标记注释**

将第 46-142 行的 Phase/Skill 标记注释清理为简洁的分组注释：

```python
# ============================================================
# Core imports
# ============================================================

from tts_engine import TTSEngine
from ai_client import call_ai as ai_client_call_ai

from config import *
from auth import *
from prompts import *
from memory_legacy import *
from weather import *
from anniversary import *
from emotion import *
from stats import *
from image_gen import *
from chat_history import *

from novel_knowledge import init_novel_knowledge

# Character system
from characters import (
    load_characters_from_dir,
    get_current_character,
    set_current_character,
    register_character,
    get_character_count,
)
from characters.base import CharacterConfig
from characters.chayewoon import Character as ChayewoonCharacter

# ============================================================
# Package imports - Command handlers
# ============================================================

from packages.commands.basic import (
    start, reset, selfie_cmd, memory_cmd, forget_cmd, search_memory_cmd,
    export_cmd, import_cmd, photo_count_cmd,
)
from packages.commands.skills import sticker_cmd, analyze_cmd, stats_cmd
from packages.commands.misc import (
    quota_cmd, quota_reset_cmd, learned_cmd, summarize_cmd,
    version_cmd, check_update_cmd, anniversary_cmd,
    call_gemini, web_search,
    check_for_updates,
)
from packages.commands.extra import (
    analyze_img_cmd, ocr_cmd,
)
from packages.commands.import_cmds import import_chat_cmd, list_imported_cmd

# ============================================================
# Package imports - Message handlers
# ============================================================

from packages.handlers.message import (
    handle_photo, handle_document, button_callback, handle_message,
    send_active_message, voice_cmd, voice_sample_cmd, voice_train_cmd, voice_status_cmd,
    music_cmd, novel_cmd, tts_voice_toggle, tts_status_cmd, qdrant_memory_cmd,
)

# ============================================================
# Package imports - Web routes
# ============================================================

from packages.web.routes import register_routes

# ============================================================
# Package imports - Video import
# ============================================================

from packages.importers.video import import_video_cmd, handle_video_import, get_video_analysis_context
from packages.analysis.chatlog import get_all_imported_relationships

# ============================================================
# Re-exports for backward compatibility
# ============================================================

from ai_core import call_ai, summarize_and_save_memory
from scheduler import scheduler
```

- [ ] **Step 3: 清理 main() 中的过时注释**

在 `main()` 函数中，清理以下行内注释：
- 第 204 行 `# [Skill: proactive-agent] 启动主动行为后台任务` → `# 主动行为后台任务`
- 第 206 行 `# [Skill: auto-updater] 启动时检查更新` → `# 启动时检查更新`
- 第 225 行 `# [Skill: TTS] 缓存清理` → `# TTS 缓存清理`
- 第 353-355 行 `# [Skill: TTS v1.4.7.2] ...` → 删除版本标记
- 第 363 行 `# [Skill: chromadb-memory] v1.4.7 语义记忆搜索` → `# 语义记忆搜索`
- 第 390 行 `# Move to selfie command (inline definition - small enough to keep here)` → `# /toselfie 命令`
- 第 447 行 `# [Skill: LightRAG] 后台加载小说知识库` → `# 后台加载小说知识库`

- [ ] **Step 4: 更新启动信息**

将第 300 行：
```python
print(f"📋 v{BOT_VERSION}: Webhook 自动部署 + systemd 服务管理")
```
改为：
```python
print(f"📋 v{BOT_VERSION}: 角色扮演 + 游戏系统 + Web 界面")
```

- [ ] **Step 5: 验证 bot.py 语法正确**

```bash
python -m py_compile bot.py && echo "bot.py syntax OK"
```

- [ ] **Step 6: Commit**

```bash
git add bot.py
git commit -m "refactor(v1.6.2.1): 清理 bot.py 过时注释，更新文件头和启动信息"
```

---

## Task 8: 更新 config.py 中的路径引用

**Files:**
- Modify: `config.py` (如果有引用被移动文件的路径)

- [ ] **Step 1: 检查 config.py 是否引用了被移动的文件**

```bash
grep -n "auth\|scheduler\|awakening\|bridge_server\|init_admin\|cloudflare\|auto_restart" config.py
```

如果 config.py 中有引用被移动的模块路径，需要更新。根据之前的分析，config.py 主要定义常量和路径，不太可能直接 import 这些模块，但需要确认。

- [ ] **Step 2: 如有修改则 commit**

```bash
git add config.py
git commit -m "fix(v1.6.2.1): 更新 config.py 中的路径引用"
```

---

## Task 9: 全量验证

- [ ] **Step 1: Python 语法检查所有修改过的文件**

```bash
cd /workspace/love-supremacy-zone
python -m py_compile bot.py && echo "bot.py OK"
python -m py_compile system/auth.py && echo "system/auth.py OK"
python -m py_compile system/scheduler.py && echo "system/scheduler.py OK"
python -m py_compile system/email_sender.py && echo "system/email_sender.py OK"
python -m py_compile characters/ai_client.py && echo "characters/ai_client.py OK"
python -m py_compile characters/ai_core.py && echo "characters/ai_core.py OK"
python -m py_compile characters/chat_history.py && echo "characters/chat_history.py OK"
python -m py_compile characters/emotion.py && echo "characters/emotion.py OK"
python -m py_compile characters/stats.py && echo "characters/stats.py OK"
python -m py_compile characters/anniversary.py && echo "characters/anniversary.py OK"
python -m py_compile characters/image_gen.py && echo "characters/image_gen.py OK"
python -m py_compile characters/memory_legacy.py && echo "characters/memory_legacy.py OK"
python -m py_compile game_api/awakening_detector.py && echo "game_api/awakening_detector.py OK"
```

- [ ] **Step 2: Import 链验证**

```bash
python -c "
# 验证所有 re-export shim
from auth import validate_user, generate_session_token, is_admin_user
from ai_client import call_ai
from ai_core import call_ai as call_ai2
from chat_history import save_chat_message
from emotion import get_emotion_emoji
from stats import load_stats
from anniversary import load_anniversaries
from image_gen import generate_face_image
from memory_legacy import load_json
from weather import get_weather
from tts_engine import TTSEngine
from scheduler import scheduler
from email_sender import send_email
from awakening_detector import AWAKENING_STAGES
print('All re-export shims OK')
"
```

- [ ] **Step 3: 验证根目录文件数量**

```bash
# 根目录应该只剩下以下 .py 文件
ls *.py
# 预期: bot.py, config.py, prompts.py, webhook_server.py
# 加上所有 re-export shim
```

预期根目录 .py 文件列表：
- `bot.py` (入口)
- `config.py` (配置)
- `prompts.py` (提示词)
- `webhook_server.py` (Webhook 服务)
- `auth.py` (shim)
- `ai_client.py` (shim)
- `ai_compete.py` (shim)
- `ai_core.py` (shim)
- `anniversary.py` (shim)
- `chat_engine.py` (shim)
- `chat_history.py` (shim)
- `emotion.py` (shim)
- `emotion_analyzer.py` (shim)
- `image_gen.py` (shim)
- `memory_legacy.py` (shim)
- `qdrant_memory.py` (shim)
- `tts_engine.py` (shim)
- `weather.py` (shim)
- `music_skill.py` (shim)
- `novel_knowledge.py` (shim)
- `stats.py` (shim)
- `scheduler.py` (shim)
- `email_sender.py` (shim)
- `awakening_detector.py` (shim)

- [ ] **Step 4: 最终 Commit**

```bash
git add -A
git commit -m "chore(v1.6.2.1): 全量验证通过，根目录整理完成"
```

---

## Self-Review 检查清单

1. **Spec 覆盖：**
   - [x] ai_client/ai_compete/ai_core → characters/ ✓
   - [x] anniversary → characters/ ✓
   - [x] auth.py + auth_v3.py 合并 → system/auth.py ✓
   - [x] auto_restart_watcher 等脚本 → system/ ✓
   - [x] awakening_detector → game_api/ ✓
   - [x] bot.py 清理过时注释 ✓
   - [x] bot_instance/manager/config → tools/_archive/ ✓
   - [x] bridge_server.py 删除 ✓
   - [x] chat_engine/chat_history → characters/ ✓
   - [x] cloudflare 脚本 → system/ ✓
   - [x] emotion/emotion_analyzer → characters/ ✓
   - [x] init_admin → tools/ ✓
   - [x] migrate_to_sqlite 删除 ✓

2. **Placeholder 扫描：** 无 TBD/TODO/Fill in details

3. **类型一致性：** 所有 import 路径在 Task 3-7 中保持一致

4. **向后兼容：** 所有移动的文件都在根目录保留了 re-export shim，34 处 auth 引用和所有 characters 模块引用不会断裂
